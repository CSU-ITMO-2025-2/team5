# 📂 Alembic Migrations

Этот каталог содержит все скрипты миграций базы данных для сервиса **Authentication Service**.

Миграции управляются инструментом **Alembic** и должны использоваться для любых изменений в схеме базы данных.

## 🛠️ Основные команды

Все команды Alembic должны выполняться из корневого каталога проекта (`auth_service/`).

### 1. Создание новой миграции (Autogenerate)

Используйте эту команду для автоматического создания файла миграции на основе изменений, внесенных в модели SQLAlchemy.

```bash
alembic revision --autogenerate -m "Краткое описание изменения, например: added_user_email_field"
```
